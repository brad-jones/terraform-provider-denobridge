---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

{{ if .HasExamples -}}
## Example Usage

{{- range .ExampleFiles }}

{{ tffile . }}
{{- end }}
{{- end }}

{{ .SchemaMarkdown | trimspace }}

## Implementation Guide

The provider communicates with your TypeScript/JavaScript code using **JSON-RPC 2.0 over stdio** (standard input/output).

For implementation details, see the [JSON-RPC Interface Guide](../guides/json-rpc-interface.md) which documents:
- Complete OpenRPC specification for the action `invoke` method and `invokeProgress` notifications
- Request/response examples with actual JSON-RPC messages
- Progress reporting patterns
- Using the `@brad-jones/terraform-provider-denobridge` JSR package with abstract base classes

**Recommended approach**: Extend `DenoBridgeAction` from the JSR package rather than implementing the JSON-RPC protocol directly.

_See also: <https://developer.hashicorp.com/terraform/plugin/framework/functions-concepts/action-functions>_