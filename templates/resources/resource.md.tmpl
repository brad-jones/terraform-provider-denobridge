---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

{{ if .HasExamples -}}
## Example Usage

{{- range .ExampleFiles }}

{{ tffile . }}
{{- end }}
{{- end }}

{{ .SchemaMarkdown | trimspace }}
{{- if or .HasImport .HasImportIDConfig .HasImportIdentityConfig }}

## Import

Import is supported using the following syntax:
{{- end }}
{{- if .HasImportIdentityConfig }}

In Terraform v1.12.0 and later, the [` + "`" + `import` + "`" + ` block](https://developer.hashicorp.com/terraform/language/import) can be used with the ` + "`" + `identity` + "`" + ` attribute, for example:

{{tffile .ImportIdentityConfigFile }}

{{ .IdentitySchemaMarkdown | trimspace }}
{{- end }}
{{- if .HasImportIDConfig }}

In Terraform v1.5.0 and later, the [` + "`" + `import` + "`" + ` block](https://developer.hashicorp.com/terraform/language/import) can be used with the ` + "`" + `id` + "`" + ` attribute, for example:

{{tffile .ImportIDConfigFile }}
{{- end }}
{{- if .HasImport }}

The [` + "`" + `terraform import` + "`" + ` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

{{codefile "shell" .ImportFile }}
{{- end }}

## Implementation Guide

The provider communicates with your TypeScript/JavaScript code using **JSON-RPC 2.0 over stdio** (standard input/output).

For implementation details, see the [JSON-RPC Interface Guide](../guides/json-rpc-interface.md) which documents:
- Complete OpenRPC specification for all resource methods (`create`, `read`, `update`, `delete`, `modifyPlan`)
- Request/response examples with actual JSON-RPC messages
- Error handling patterns
- Using the `@brad-jones/terraform-provider-denobridge` JSR package with abstract base classes

**Recommended approach**: Extend `DenoBridgeResource` from the JSR package rather than implementing the JSON-RPC protocol directly.

_See also: <https://developer.hashicorp.com/terraform/plugin/framework/resources>_

